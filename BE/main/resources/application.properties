spring.application.name=DatVeXemPhim
# ===================================================================
# SERVER CONFIGURATION
# ===================================================================
spring.threads.virtual.enabled=true
server.port=${SERVER_PORT:9999}
spring.banner.location=banner/banner.txt
server.version=${SERVER_VERSION:1.0.0}
allowed.origin=${ALLOWED_ORIGIN:http://localhost:6789}

# FRONTEND INFORMATION
frontend.url=${FRONTEND_URL:http://localhost:6789}

# ===================================================================
# DB GENERATOR (Kh?i t?o t�i kho?n Admin m?c ??nh)
# ===================================================================
db.generator.is-generated=${GENERATE_DB:true}
db.generator.user-email=${USER_EMAIL:nguyenquochuy8606@gmail.com}
db.generator.user-code=${USER_CODE:AD_1234}
db.generator.user-name=${USER_NAME:Nguyen Qu?c Huy}
db.generator.user-phone=${USER_PHONE:0984832792}

# TOMCAT CONFIGURATION
server.tomcat.threads.max=400
server.tomcat.threads.min-spare=20

# ===================================================================
# DATABASE CONFIGURATION (SQL SERVER - DatnMovieTicketing)
# ===================================================================
spring.datasource.url=jdbc:sqlserver://${SQL_SERVER_HOST:localhost}:${SQL_SERVER_PORT:1433};databaseName=${SQL_SERVER_DATABASE:DatnMovieTicketing};encrypt=true;trustServerCertificate=true;
spring.datasource.username=${SQL_SERVER_USER:sa}
spring.datasource.password=${SQL_SERVER_PASSWORD:Huy@123456}
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ===================================================================
# JPA / HIBERNATE CONFIGURATION
# ddl-auto=none ?? kh�ng l�m h?ng c?u tr�c b?ng ?� t?o b?ng script SQL
# ===================================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.globally_quoted_identifiers=false
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:none}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true

# Optimization for Batch Processing
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true
spring.jpa.properties.hibernate.jdbc.batch_size=100

# Config Batch Processing
spring.batch.jdbc.initialize-schema=always
spring.batch.job.enabled=false

# ===================================================================
# SECURITY CONFIGURATION (JWT & OAuth2)
# ===================================================================
#jwt.secret=
#
#spring.security.oauth2.client.registration.google.client-name=
spring.security.oauth2.client.registration.google.client-name=Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# WEB SOCKET CONFIGURATION (D�ng cho th�ng b�o ??t v� realtime)
ws.applicationPrefix=/app
ws.topicPrefix=/topic
ws.registerEndpoint=/ws

# ===================================================================
# FILE UPLOAD EXCEL (?� ??i t�n chu?n theo DB B�n V�)
# ===================================================================
file.upload.movie.path=${FILE_UPLOAD_MOVIE_PATH:src/main/resources/movie-excel}
file.upload.cinema.path=${FILE_UPLOAD_CINEMA_PATH:src/main/resources/cinema-excel}
file.upload.schedule.path=${FILE_UPLOAD_SCHEDULE_PATH:src/main/resources/schedule-excel}

# Job Cron: Ch?y m?i 30 gi�y ?? quy�t file Excel (n?u c?n)
excel.file.to.database.job.cron=0/30 * * * * *

# ===================================================================
# CLOUDINARY (L?u tr? ?nh Poster Phim, Banner, Avatar)
# ===================================================================
cloudinary.cloud-name=
cloudinary.api-key=
cloudinary.api-secret=

# Servlet Multipart Config
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Schedule (Ch?y v�o 00:00 m?i ng�y - Cnpm run dev� th? d�ng ?? c?p nh?t tr?ng th�i phim)
schedule.project.cron=${PROJECT_UPDATE_CRON:0 0 0 * * ?}

spring.messages.encoding=UTF-8
auth.token.secret=${AUTH_TOKEN_SECRET:}

# ===================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# L?u : C?n t?o App Password m?i cho email nguyenquochuy8606@gmail.com
# ===================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenquochuy8606@gmail.com
spring.mail.password=fnveydnwnkmldkao
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# ===================================================================
# SYSTEM & LOGGING
# ===================================================================
server.forward-headers-strategy=framework
spring.main.headless=true
java.awt.headless=true

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.client.RestTemplate=DEBUG
logging.level.org.springframework.web=DEBUG

# ===================================================================
# REDIS CACHE CONFIGURATION
# ===================================================================
redis.host=${REDIS_HOST:localhost}
redis.port=${REDIS_PORT:6378}
spring.cache.type=redis
spring.cache.redis.cache-null-values=true
spring.cache.redis.time-to-live=20
logging.level.org.springframework.data=ERROR
